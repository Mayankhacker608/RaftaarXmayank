{
  "name": "raftaarx-workspace",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "build": "node scripts/build-all.mjs",
    "vercel-build": "npm run build",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
